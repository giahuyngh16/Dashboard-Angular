const ROLES = {
  ADMIN: 'Admin',
  STAFF: 'Staff',
  MANAGER: 'Manager',
  CUSTOMER: 'Customer',
  SHIPPER: 'Shipper',
};

const ALL_ROLES = [
    ROLES.ADMIN, ROLES.STAFF, ROLES.CUSTOMER, ROLES.MANAGER, ROLES.SHIPPER
];

export { ROLES, ALL_ROLES };
